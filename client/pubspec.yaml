name: client
description: "Flutter клиент для BDUI челленджей"

version: 1.0.0+1

publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0 
  dio: ^5.9.0
  retrofit: ^4.5.0
  flutter_bloc: ^8.1.3
  get_it: ^7.6.4
  json_annotation: ^4.9.0
  shared:
    path: ../shared


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  retrofit_generator: ^9.1.5
  build_runner: ^2.4.13
  json_serializable: ^6.9.0

flutter:
  uses-material-design: true